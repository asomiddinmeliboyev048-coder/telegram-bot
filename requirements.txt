# Render Optimized Requirements
# Core async Telegram bot
pyTelegramBotAPI>=4.15.0

# Media processing
yt-dlp>=2025.4.0
edge-tts>=6.1.9

# Environment
python-dotenv>=1.0.0

# Optional: Spotify (lazy loaded)
spotipy>=2.3.0

# Async support (included with pyTelegramBotAPI)
aiohttp>=3.9.0

# REQUIRED: Flask for Render port binding (keeps bot alive)
flask>=3.0.0
gunicorn>=21.0.0

# Note: requests removed - not needed for webhook-less bot
# Note: aiofiles removed - using built-in async methods
